package main

import (
	"fmt"
	"time"
)

func main() {
	fmt.Println("Hello World! ", time.Now()) // formatted print
	//print("Hello Universe!\n")
	// os.exit(1)
	// auto return as status 0
}

// gdb, lldb, delve
// dwarf, symbol
// release build
// go build -ldflags
